# DANH SÁCH LỖI & TESTCASE GIAO DIỆN - TRANG CHỦ GOBIKE

> **Mục đích**: Ghi chép chi tiết toàn bộ các lỗi giao diện, sai lệch hiển thị và yêu cầu tinh chỉnh theo từng Section trên Trang chủ (Desktop & Mobile).
> **Quy chuẩn chung**:
> - Màu xanh chủ đạo: `#149d29`
> - Không dùng `box-shadow` (loại bỏ toàn bộ bóng đổ)
> - `font-weight` tối đa là `700`

---

## 1. HEADER & TOPBAR NAVIGATION
- **Đường dẫn lưu code**:
  - **CSS Giao diện**: [`wp-content/themes/flatsome-child/shortcodes/home/home-styles.php`](file:///e:/1.%20D%E1%BB%B0%20%C3%81N%20TH%E1%BB%B0C%20T%E1%BA%BE%20%282026%29/%2817%29%20GOBIKE%20%2828082026%29/wp-content/themes/flatsome-child/shortcodes/home/home-styles.php)
  - **Placeholder & Script xử lý**: [`wp-content/themes/flatsome-child/functions.php`](file:///e:/1.%20D%E1%BB%B0%20%C3%81N%20TH%E1%BB%B0C%20T%E1%BA%BE%20%282026%29/%2817%29%20GOBIKE%20%2828082026%29/wp-content/themes/flatsome-child/functions.php)

- [x] **Lỗi 1 (Đồng bộ giao diện thanh tìm kiếm Header theo đúng mẫu thiết kế)**:
  - **Khung viền thanh tìm kiếm**: Bo góc chuẩn 8px (`border: 1.5px solid #dbe2ea; border-radius: 8px; height: 42px; background: #ffffff; padding: 3px 3px 3px 14px;`), loại bỏ hoàn toàn `box-shadow` và triệt tiêu pseudo-element `:before` mặc định của theme Flatsome `li.html:before` gây che lấp/lệch lề.
  - **Ô nhập từ khóa (Search field)**: Nền trong suốt, không viền, không outline, font-size `14px`, text màu xám đậm `#1e293b`.
  - **Placeholder chuẩn thiết kế mẫu**: Thiết lập nội dung placeholder thành `"Bạn đang tìm xe đạp trợ lực điện nào?"` (màu slate `#596377`, font-size 13.5px) đồng bộ thông qua `theme_mod_search_placeholder`, `get_product_search_form` và script client.
  - **Nút tìm kiếm màu xanh bo góc (Search button)**: Nằm lọt bên trong mép phải của thanh tìm kiếm, kích thước `38px x 34px`, bo góc nhẹ `6px`, màu xanh thương hiệu `#0d6e2e` (khi hover chuyển sang `#149d29`), chứa icon kính lúp màu trắng sắc nét căn giữa.
  - **Loại bỏ hiệu ứng nhảy/nổi (Hover)**: Giữ cố định form và nút bấm (`transform: none !important; box-shadow: none !important; margin: 0 !important; line-height: 1.2 !important;`).
  - **Bảng gợi ý tìm kiếm tức thì (Live search results)**: Bo tròn 8px, viền sáng `#e2e8f0`, ảnh sản phẩm vuông bo góc 6px, hiển thị giá bán màu xanh `#149d29`.
- [x] **Lỗi 2 (Nền Header trên Mobile/Tablet chuyển sang màu xanh #149d29 chuẩn Ảnh 1)**:
  - **Nền Header**: Thiết lập màu nền `#149d29` cho toàn bộ thanh header (`#header`, `.header-wrapper`, `.header-main`, `.header-bottom`) trên màn hình mobile/tablet ($\le 849\text{px}$).
  - **Hệ thống Icon**: Chuyển các icon Menu Hamburger, Tài khoản, Giỏ hàng sang màu trắng (`#ffffff`), huy hiệu số lượng giỏ hàng màu đỏ nổi bật.
  - **Logo thương hiệu**: Tự động chuyển logo sang màu trắng sắc nét trên nền xanh thương hiệu.
  - **Thanh tìm kiếm**: Thiết kế nền trắng bo góc tròn 20px, giữ nút kính lúp xanh `#0d6e2e` bên trong.

---

## 2. HERO BANNER & SLIDER (`[gobike_home_hero_banner]`)
- **Đường dẫn lưu code**:
  - **File Shortcode PHP**: [`wp-content/themes/flatsome-child/shortcodes/home/sc-hero-banner.php`](file:///e:/1.%20D%E1%BB%B0%20%C3%81N%20TH%E1%BB%B0C%20T%E1%BA%BE%20%282026%29/%2817%29%20GOBIKE%20%2828082026%29/wp-content/themes/flatsome-child/shortcodes/home/sc-hero-banner.php)
  - **File Page Template**: [`wp-content/themes/flatsome-child/page-blank.php`](file:///e:/1.%20D%E1%BB%B0%20%C3%81N%20TH%E1%BB%B0C%20T%E1%BA%BE%20%282026%29/%2817%29%20GOBIKE%20%2828082026%29/wp-content/themes/flatsome-child/page-blank.php)
  - **CSS Giao diện**: [`wp-content/themes/flatsome-child/shortcodes/home/home-styles.php`](file:///e:/1.%20D%E1%BB%B0%20%C3%81N%20TH%E1%BB%B0C%20T%E1%BA%BE%20%282026%29/%2817%29%20GOBIKE%20%2828082026%29/wp-content/themes/flatsome-child/shortcodes/home/home-styles.php) *(Mục 6: TRANG CHỦ: BANNER SLIDER & TIN TỨC)*

- [x] **Lỗi 1 (Đồng bộ giao diện Hero Banner & Slider theo chuẩn thiết kế Ảnh 1)**:
  - **Bổ sung Cột 1 (Banner dọc Chất lượng thật bền vạn năm bên trái)**: Tái kích hoạt `.box_left` thành thẻ card dọc bo tròn 12px, nền xanh lá thương hiệu, hiển thị 3 khung oval hình người lái xe và badge bảo đảm chất lượng bền vạn năm, chiều cao 380px cân bằng tuyệt đối với khối giữa và cột tin tức.
  - **Khối giữa (Slider & Dải 4 dịch vụ cam kết)**:
    + **Bỏ mục "Khuyến mãi xe đạp điện"** (được gạch chéo đỏ trên ảnh yêu cầu), tinh gọn từ 5 mục xuống đúng 4 mục dịch vụ cam kết: (1) Xe đạp thể thao mới, (2) Phụ kiện xe đạp chính hãng, (3) Xe đạp trẻ em an toàn, (4) Dịch vụ bảo dưỡng miễn phí.
    + Thêm icon trực quan cho từng mục dịch vụ, chia đều 4 cột (mỗi cột 25% chiều rộng), hiển thị 2 dòng chữ in hoa rõ ràng, chuyển màu xanh lá và active đồng bộ 1-to-1 với Swiper Slider.
  - **Cột phải (Tin tức mới nhất & Banner Showroom GoBike)**:
    + Bổ sung nút/link **`Xem tất cả →`** nằm cạnh tiêu đề "TIN TỨC MỚI NHẤT" liên kết trực tiếp tới `/tin-tuc/`.
    + 3 bài viết hiển thị ảnh đại diện bo góc 6px, tiêu đề in đậm rõ ràng, ngày đăng phân cách kẻ mờ.
    + Thay thế ảnh cũ "Giá trị cốt lõi" bằng **Banner Showroom GoBike chuyên nghiệp**, chứa đầy đủ thông tin hệ thống showroom và nút "Tìm showroom gần bạn →" liên kết trang liên hệ.
- [x] **Lỗi 2 (Tối ưu Banner Home trên Mobile/Tablet: Đầy màn ngang, tăng chiều cao & hiển thị 4 khối dịch vụ chuẩn Ảnh 2)**:
  - **Tràn viền đầy màn ngang**: Triệt tiêu padding 10px của `.banner-home`, đưa container và row về `width: 100%; padding: 0; margin: 0;`, loại bỏ viền bo góc thừa giúp banner phủ kín 100% chiều rộng màn hình.
  - **Tăng chiều cao Banner**: Tăng chiều cao banner lên `250px` (mobile) và `320px` (tablet), `object-fit: cover; object-position: center;` giúp hiển thị trọn vẹn bố cục banner, không bị lùn/co ép ảnh.
  - **Hiển thị 4 khối dịch vụ/cam kết bên dưới Banner**: Kích hoạt hiển thị `.banner-home .mySwiper.gobike-service-thumbs` dàn 4 cột trong 1 hàng (mỗi cột 25% chiều rộng) gồm icon SVG và text 2 dòng, phân cách đường kẻ mờ `#f1f5f9`, tự động đồng bộ highlight xanh khi chuyển slide.

---

## 3. SECTION: BỘ LỌC TÌM XE NHANH 60 GIÂY (`[gobike_quick_finder]`)
- **Đường dẫn lưu code**:
  - **File Shortcode PHP (HTML, CSS & JS)**: [`wp-content/themes/flatsome-child/shortcodes/home/sc-quick-finder.php`](file:///e:/1.%20D%E1%BB%B0%20%C3%81N%20TH%E1%BB%B0C%20T%E1%BA%BE%20%282026%29/%2817%29%20GOBIKE%20%2828082026%29/wp-content/themes/flatsome-child/shortcodes/home/sc-quick-finder.php)
  - **File nạp Shortcode**: [`wp-content/themes/flatsome-child/functions.php`](file:///e:/1.%20D%E1%BB%B0%20%C3%81N%20TH%E1%BB%B0C%20T%E1%BA%BE%20%282026%29/%2817%29%20GOBIKE%20%2828082026%29/wp-content/themes/flatsome-child/functions.php)

- [x] **Lỗi 1 (Đồng bộ giao diện Bộ lọc tìm xe nhanh 60 giây theo chuẩn thiết kế Ảnh 2)**:
  - **Padding khung viền**: Thu gọn padding của `.gqf-container` từ `24px 28px 20px 28px` xuống chuẩn gọn `16px 20px 14px 20px`, giảm khoảng cách `margin-bottom` của khối trên xuống 14px giúp khung thanh thoát, không bị khoảng trống quá lớn.
  - **Tiêu đề `.gqf-title`**: Nâng font-size lên **`34px`** (font-weight 700, line-height 1.08), phối màu chữ "60 GIÂY" màu vàng nổi bật (`#facc15`) và "CHỌN ĐÚNG XE" màu trắng chuẩn thiết kế mẫu.
  - **Mô tả `.gqf-desc`**: Tăng kích thước từ 12.5px lên **`16px`** (line-height 1.45, độ tương phản cao), mở rộng chiều rộng cột giới thiệu lên 280px.
  - **Khối 4 bước `.gqf-steps` & `.gqf-step-item`**:
    + Nâng cấp các thẻ bước thành dạng card bo tròn (`background: rgba(0, 0, 0, 0.16); border: 1px solid rgba(255, 255, 255, 0.12); border-radius: 14px; width: 125px; min-height: 125px;`).
    + Đặt số thứ tự (1, 2, 3, 4) nằm ở góc trên bên trái card (`top: 8px; left: 8px; width: 22px; height: 22px; background: #fff; color: #0f172a; border-radius: 50%; font-weight: 700;`).
    + Tăng kích thước vòng tròn icon giữa lên `48px x 48px` và icon vector SVG lên `22px x 22px`.
    + Cỡ chữ nhãn bước `.gqf-step-label` tăng lên `13px` (font-weight 600).
  - **Nút CTA "BẮT ĐẦU TƯ VẤN 60S"**: Nâng kích thước chữ lên **`15px`**, in đậm `font-weight: 700`, màu vàng nhận diện `#facc15`, text màu đen than `#0f172a`, bo tròn 30px nổi bật.
- [x] **Lỗi 2 (Loại bỏ hiệu ứng hover nhấc nổi / bay lên của các nút bấm)**:
  - Loại bỏ hoàn toàn thuộc tính `transform: translateY(...)` khi rê chuột (hover) vào nút "BẮT ĐẦU TƯ VẤN 60S" (`.gqf-btn-primary`), nút "TÌM XE PHÙ HỢP" (`.gqf-btn-tim-xe`) và các thẻ bước (`.gqf-step-item`).
  - Nút giữ nguyên vị trí cố định, chỉ đổi màu nền nhẹ nhàng (`background-color transition`), tạo cảm giác chắc chắn, chuyên nghiệp và không bị giật/nhấc nổi giao diện.
- [x] **Lỗi 3 (Sửa các lỗi hiển thị trên thanh Bộ lọc & Modal Popup tư vấn 60 giây)**:
  - **Khắc phục lỗi nền trắng khi chọn option trên thanh lọc (Ảnh 2)**: Khóa `background: transparent !important; color: #ffffff !important;` cho `.gqf-select` trên tất cả các trạng thái `:focus`, `:active`, `:hover`, `:visited`, loại bỏ hoàn toàn hiện tượng Flatsome tự ý gán nền trắng đè lên khi chọn option; đồng thời bổ sung sự kiện `change -> this.blur()` giải phóng focus ngay khi hoàn tất lựa chọn.
  - **Căn giữa thẳng hàng các icon trong ô input modal (Ảnh 4)**: Cố định vị trí `.gqf-input-icon` với `top: 50% !important; transform: translateY(-50%) !important; left: 14px !important; z-index: 5 !important;`, gán `margin: 0 !important; padding: 0 14px 0 40px !important; height: 42px !important;` cho `.gqf-input`, triệt tiêu hoàn toàn `margin-bottom` và `padding` mặc định của theme Flatsome làm icon rơi xuống mép đáy ô nhập liệu.
  - **Chuẩn hóa nút chip nhu cầu sử dụng `.gqf-chip`**: Loại bỏ toàn bộ margin thừa (`margin: 0 !important;`), gán `line-height: 1.2 !important; font-size: 13px !important;`, chip active mang nền xanh nhạt `#dcfce7` kèm viền xanh `#149d29`.
  - **Đồng bộ 2 nút hành động dưới đáy modal bằng nhau 50% - 50% (Ảnh 3 & Ảnh 5)**: Chuyển `.gqf-form-actions` sang `grid-template-columns: 1fr 1fr !important; gap: 12px !important;`. Nút "Để sau" (`.gqf-btn-cancel`) và nút "Nhận tư vấn ngay →" (`.gqf-btn-submit`) có cùng chiều cao 44px, độ rộng 100% của cột, bo góc 8px, `font-weight: 700`, `margin: 0 !important; line-height: 1.2 !important;` chuẩn khớp 100% theo giao diện mẫu Ảnh 5.
- [ ] **Lỗi 4**: 

---

## 4. SECTION: VIDEO SHORTS / TRẢI NGHIỆM XE THỰC TẾ (`[gobike_experience_videos]`)
- **Đường dẫn lưu code**:
  - **File Shortcode PHP (HTML, CSS & JS)**: [`wp-content/themes/flatsome-child/shortcodes/home/sc-experience-videos.php`](file:///e:/1.%20D%E1%BB%B0%20%C3%81N%20TH%E1%BB%B0C%20T%E1%BA%BE%20%282026%29/%2817%29%20GOBIKE%20%2828082026%29/wp-content/themes/flatsome-child/shortcodes/home/sc-experience-videos.php)
  - **File nạp Shortcode**: [`wp-content/themes/flatsome-child/functions.php`](file:///e:/1.%20D%E1%BB%B0%20%C3%81N%20TH%E1%BB%B0C%20T%E1%BA%BE%20%282026%29/%2817%29%20GOBIKE%20%2828082026%29/wp-content/themes/flatsome-child/functions.php)

- [x] **Lỗi 1 (Tinh chỉnh hiển thị khối Video Shorts & Sản phẩm đính kèm)**:
  - **Bỏ dấu gạch ở tiêu đề**: Xóa bỏ hoàn toàn thẻ `<span class="gev-bar-prefix">|</span>` khỏi tiêu đề chính section.
  - **Khoảng cách các item 8px**: Thu hẹp `gap` của `.gev-grid` từ 20px xuống `8px !important;`, các cột phân bố đều và khít đẹp mắt trên mọi độ phân giải.
  - **Bỏ mô tả trên ảnh (alt)**: Xóa bỏ thẻ chữ nổi `.gev-overlay-top` và xóa nội dung `alt` trên ảnh thumbnail video để không hiện tooltip chữ đè lên ảnh.
  - **Khối sản phẩm ảnh tỉ lệ 1x1 & padding 8px**: Mở rộng khung ảnh sản phẩm lên `66px x 66px`, gán `aspect-ratio: 1 / 1 !important; object-fit: contain !important;`, giảm padding của `.gev-product-box` về `8px !important; gap: 8px !important;`.
  - **Typography sản phẩm**: Nâng cỡ chữ tiêu đề sản phẩm `.gev-prod-name` lên **`16px`** (`font-weight: 700`), hạ cỡ chữ danh mục bên dưới `.gev-prod-cat` về **`14px`** (`font-weight: 400`, màu xám `#64748b`).
  - **Thay thế chữ 'Xem giỏ hàng' bằng icon**: Ẩn triệt để text link `.added_to_cart` mà WooCommerce tự động chèn vào (`display: none !important;`); thay vào đó nút giỏ hàng đổi sang nền xanh `#149d29` và hiển thị icon dấu tích checkmark SVG gọn gàng.
  - **Bỏ margin & đồng bộ chiều cao các khối bằng nhau**: Loại bỏ margin thừa trên card và nút bấm (`margin: 0 !important; line-height: 1.2 !important;`), thiết lập `align-items: stretch` và `margin-top: auto` cho `.gev-product-box` giúp toàn bộ các khối card luôn có chiều cao bằng nhau tuyệt đối.
  - **Nút đóng video nền trắng dấu x đen**: Tạo nút đóng `.gev-inline-close-btn` tròn nền trắng (`#ffffff`), viền mỏng tinh tế, icon dấu X đen (`#000000`) nằm ở góc trên bên phải (`top: 10px; right: 10px; z-index: 9999;`), dễ thao tác và không bị che khuất.
- [ ] **Lỗi 2**: 

---

## 5. SECTION: DANH MỤC THƯƠNG HIỆU & THẺ SẢN PHẨM (`[gobike_category_block]`)
- **Đường dẫn lưu code**:
  - **File Shortcode PHP (HTML & Query)**: [`wp-content/themes/flatsome-child/shortcodes/home/sc-category-block.php`](file:///e:/1.%20D%E1%BB%B0%20%C3%81N%20TH%E1%BB%B0C%20T%E1%BA%BE%20%282026%29/%2817%29%20GOBIKE%20%2828082026%29/wp-content/themes/flatsome-child/shortcodes/home/sc-category-block.php)
  - **File CSS Giao diện**: [`wp-content/themes/flatsome-child/shortcodes/home/home-styles.php`](file:///e:/1.%20D%E1%BB%B0%20%C3%81N%20TH%E1%BB%B0C%20T%E1%BA%BE%20%282026%29/%2817%29%20GOBIKE%20%2828082026%29/wp-content/themes/flatsome-child/shortcodes/home/home-styles.php) *(Đoạn `/* 4. KHỐI DANH MỤC SẢN PHẨM */` từ dòng 730 đến 1290)*
  - **File nạp Shortcode**: [`wp-content/themes/flatsome-child/functions.php`](file:///e:/1.%20D%E1%BB%B0%20%C3%81N%20TH%E1%BB%B0C%20T%E1%BA%BE%20%282026%29/%2817%29%20GOBIKE%20%2828082026%29/wp-content/themes/flatsome-child/functions.php)

- [x] **Lỗi 1 (Màu tiêu đề sản phẩm)**: Đã chuyển tên xe (`.gobike-pcard-title a`) về màu tối tự nhiên (`#1e293b`), chỉ chuyển sang màu xanh `#149d29` khi hover.
- [x] **Lỗi 2 (Kích thước tiêu đề sản phẩm & bỏ height)**: Đã nâng font-size lên `18px`, loại bỏ toàn bộ các thuộc tính `height` và `min-height` cố định trên tất cả các tiêu đề sản phẩm.
- [x] **Lỗi 3 (Bỏ khối icon thương hiệu header-brand-badge)**: Đã xóa bỏ thẻ `<span class="header-brand-badge">` khỏi tiêu đề khối danh mục.
- [x] **Lỗi 4 (3 thông số nằm trên 1 hàng, bằng nhau & text ellipsis)**: Đã thiết lập grid 3 cột bằng nhau (`grid-template-columns: repeat(3, minmax(0, 1fr))`), nếu thông số dài vượt quá sẽ tự động hiển thị dấu ba chấm (`...`).
- [x] **Lỗi 5 (Chuẩn hóa font-size theo bội số của 4)**: Đã thay các cỡ chữ lẻ (11.5px, 14.5px, 11px, 13.5px) thành các bội số chẵn của 4 (12px, 16px, 18px, 20px).
- [x] **Lỗi 6 (Ảnh sản phẩm bị bó cứng cố định `max-height: 135px`)**: Đã bỏ `max-height: 135px`, chuyển sang `width: 100%; height: 100%; object-fit: contain;` với padding đệm 8px cân đối.
- [x] **Lỗi 7 (Hiển thị cụm giá & cơ chế rơi dòng / ellipsis thông minh)**: Đã cấu trúc cụm giá dạng `flex-wrap: wrap !important;` và **bỏ hoàn toàn `min-height`** giúp chiều cao khối giá co giãn tự nhiên theo nội dung (không bị khoảng trống thừa khi có 1 dòng giá). Ở trạng thái thông thường, giá đỏ và giá cũ nằm chung 1 hàng; khi giá dài sẽ tự động rơi giá cũ xuống dòng 2; giá đỏ vượt 100% bề rộng dòng sẽ tự hiển thị dấu ba chấm (`...`). Thiết lập `margin-top: auto;` cho nút `.gobike-pcard-btn` đảm bảo tất cả các nút bấm luôn hít sát đáy thẻ đều nhau.
- [x] **Lỗi 8 (Chiều cao các khối thẻ sản phẩm không bằng nhau trên Desktop, Tablet & Mobile)**:
  - Thiết lập `height: 100%` và `align-items: stretch` trên các cột lưới sản phẩm và swiper-slide.
  - Cố định `min-height: 48px` (desktop) và `min-height: 36px - 40px` (tablet/mobile) cho `.gobike-pcard-title` giúp 3 ô thông số kỹ thuật thẳng hàng ngang tuyệt đối trên mọi thẻ.
  - Thiết lập `min-height: 24px` (desktop) / `22px` (mobile) và `margin-top: auto` cho `.gobike-pcard-price-box` đảm bảo sản phẩm không có giá (liên hệ) vẫn giữ khoảng trống giá bằng với sản phẩm có giá.
  - Gán `margin-top: auto` cho nút `.gobike-pcard-btn` giúp tất cả các nút luôn hít sát mép đáy thẻ đều tăm tắp.
- [ ] **Lỗi 9 (Đồng bộ cấu trúc nút thẻ theo chuẩn Ảnh 2)**: Hiện tại là nút outline "Xem chi tiết" full-width, cần đồng bộ chuẩn Ảnh 2 gồm nút "Xem chi tiết" nền xanh `#149d29` chữ trắng + nút icon giỏ hàng nhỏ bên cạnh.
- [ ] **Lỗi 10 (Màu nền Banner cột phải)**: Banner đang dùng dải nền màu xanh than tối (`#0b2f4c` -> `#0a1f2e`), cần điều chỉnh hòa hợp với bộ nhận diện màu xanh `#149d29` của GoBike.

---

## 6. SECTION: VIDEO REVIEW THỰC TẾ (`[gobike_video_reviews]`)
- **Đường dẫn lưu code**:
  - **File Shortcode PHP (HTML, CSS & JS)**: [`wp-content/themes/flatsome-child/shortcodes/home/sc-video-reviews.php`](file:///e:/1.%20D%E1%BB%B0%20%C3%81N%20TH%E1%BB%B0C%20T%E1%BA%BE%20%282026%29/%2817%29%20GOBIKE%20%2828082026%29/wp-content/themes/flatsome-child/shortcodes/home/sc-video-reviews.php)
  - **File nạp Shortcode**: [`wp-content/themes/flatsome-child/functions.php`](file:///e:/1.%20D%E1%BB%B0%20%C3%81N%20TH%E1%BB%B0C%20T%E1%BA%BE%20%282026%29/%2817%29%20GOBIKE%20%2828082026%29/wp-content/themes/flatsome-child/functions.php)

- [x] **Lỗi 1 (Định dạng cụm tabs lọc `.gvr-filter-tabs` & `.gvr-tab-btn`)**:
  - Thiết lập kích thước thẻ `svg`: `width: 16px; height: 16px; display: block;` (chuẩn bội số của 4).
  - Bỏ toàn bộ `margin` của khối `.gvr-tab-btn` (`margin: 0 !important;`).
  - Thẻ `span` được thiết lập `font-size: 14px; font-weight: 500; line-height: 1.2;`.
- [x] **Lỗi 2 (Khối tabs nhiều danh mục bị tràn đẩy nút "Xem tất cả" xuống dòng)**: Chuyển cụm `.gvr-filter-tabs` sang cơ chế cuộn ngang tự nhiên (`overflow-x: auto; flex-wrap: nowrap; min-width: 0;`), ẩn thanh cuộn thô (`scrollbar-width: none;`), các nút tab giữ nguyên không bị co méo chữ (`flex-shrink: 0; white-space: nowrap;`). Khóa `.gvr-header-wrap` dạng `flex-wrap: nowrap;` và neo cố định nút "Xem tất cả video" ở góc phải (`flex-shrink: 0; margin-left: auto;`), tích hợp thêm tính năng cuộn ngang bằng con lăn chuột (mouse wheel) trên desktop và vuốt chạm mượt mà trên mobile.
- [x] **Lỗi 3 (Khoảng cách giữa các khối video card)**: Đồng bộ khoảng cách giữa tất cả các khối bài viết video (`.gvr-main-grid` và `.gvr-side-list`) thành `gap: 8px;` (chuẩn bội số 4), đồng nhất với khoảng cách lưới sản phẩm Section 5.
- [x] **Lỗi 4 (Cụm meta chân thẻ `.gvr-ch-meta` xếp thành 2 hàng)**: Chuyển `.gvr-ch-meta` sang `flex-direction: column; align-items: flex-start; gap: 6px;` để số lượt xem/thời gian ở hàng 1 và nhãn badge ở hàng 2; chuẩn hóa font-size `12px` (bội số 4) giúp thẻ thoáng đãng, không bị chật chội hay ép chữ.
- [x] **Lỗi 5 (Khối video nổi bật `.gvr-featured-card`: Tiêu đề, mô tả & hàng cam kết)**: Giới hạn tiêu đề bài viết tối đa 2 dòng (`-webkit-line-clamp: 2`), mô tả bài viết tối đa 3 dòng (`-webkit-line-clamp: 3`). Bỏ hoàn toàn margin của nút `.gvr-btn-watch` (`margin: 0 !important;`). Chuyển toàn bộ 3 khối cam kết (icon tròn xanh, text 2 dòng, vạch ngăn cách) cùng nút "Xem video ngay" nằm gọn gàng trên cùng 1 hàng ngang chuẩn giao diện mẫu.
- [x] **Lỗi 6 (Triệt tiêu line-height nút .gvr-btn-watch và chống chèn lấn các khối cam kết)**: Bỏ line-height cao mặc định của theme trên button (`line-height: 1 !important; min-height: unset !important; height: auto !important;`), điều chỉnh tỷ lệ lưới cột video lớn thành `1.25fr 1fr` và tối ưu kích thước icon 26px, font text 10-11px giúp 3 khối cam kết và nút bấm dàn đều trên 1 hàng, hoàn toàn không bị chồng lấn hay che khuất chữ.
- [x] **Lỗi 7 (Nút đóng Popup Video Modal `.gvr-vm-close`)**: 
  - Chuyển vị trí nút đóng từ bên trong video (che các nút điều khiển YouTube) ra **góc trên bên phải bên ngoài viền video** (`top: -12px; right: -12px;`).
  - Chuyển `overflow: hidden;` của `.gvr-vm-dialog` sang `.gvr-vm-content` để video vẫn bo tròn góc 16px đẹp mắt mà nút đóng không bị che khuất viền.
  - Thu nhỏ kích thước nút đóng chuẩn gọn (`28px x 28px`), thiết kế dạng **nền trắng tròn** (`background: #ffffff; border-radius: 50%; border: 1px solid #cbd5e1; box-shadow: none !important;`) và **dấu X màu đen** sắc nét bằng vector SVG (kích thước `12px x 12px`, nét vẽ `stroke-width: 2.5`).
- [x] **Lỗi 8 (Tinh chỉnh khối Marketing "Tại sao nên xem?" `.gvr-why-watch-wrap`)**:
  - Tăng kích thước khối icon tròn `.gvr-ww-icon` từ 36px lên **`44px x 44px`**, SVG từ 20px lên **`24px x 24px`** sắc nét nổi bật.
  - Tinh chỉnh padding thẻ `.gvr-ww-card` về chuẩn **`10px`** và khoảng cách lưới `.gvr-ww-grid` thành **`gap: 8px;`** (đồng bộ chuẩn bội số 4).
  - Tăng kích thước chữ tiêu đề thẻ `.gvr-ww-content h4` lên **`18px`** (font-weight 700) và mô tả `.gvr-ww-content p` lên **`14px`** rõ ràng, dễ đọc.
  - Đổi nền nhãn trích dẫn `.gvr-ww-tag` sang **nền xanh nhẹ** (`background: #e8f5e9; color: #149d29; border-radius: 10px;`) hài hòa bộ nhận diện GoBike.
- [x] **Lỗi 9 (Đồng bộ chiều cao bằng nhau tuyệt đối giữa Video nổi bật và lưới 4 video phụ)**:
  - Khắc phục lỗi đáy khối video nổi bật bên trái không bằng đáy của 4 video bên phải (tạo khoảng hụt phía dưới), cũng như các video trong cùng 1 hàng không bằng chiều cao nhau do chênh lệch độ dài tiêu đề.
  - Xử lý: Áp dụng cơ chế co dãn tự nhiên của CSS Grid trên `.gvr-main-grid` (cả 2 cột luôn bằng chiều cao nhau), đồng thời cấu hình `.gvr-side-list` dạng `grid-template-rows: repeat(2, minmax(0, 1fr)); height: 100%;` và các thẻ `.gvr-card-horizontal` dạng `height: 100%;`. Nhờ đó:
    + 2 thẻ ở hàng 1 luôn bằng chiều cao nhau.
    + 2 thẻ ở hàng 2 luôn bằng chiều cao nhau.
    + Tổng chiều cao của 4 thẻ phụ luôn bằng chính xác 100% chiều cao của video nổi bật bên trái, đáy 2 cột thẳng hàng đều tăm tắp.
    + Khi lọc tab có ít bài viết (1-2 bài), bài viết ở hàng 1 chỉ chiếm đúng 50% chiều cao (1fr), không bao giờ bị kéo dãn thành tháp dài 500px.
    + Tự động chuyển sang `height: auto; grid-template-rows: auto;` linh hoạt trên tablet và mobile.
- [x] **Lỗi 10 (Chuyển sang dạng slide vuốt ngang trên tablet & mobile, giữ nguyên 100% desktop)**:
  - **Giao diện Desktop ($\ge 1025\text{px}$)**: Giữ nguyên vẹn 100% bố cục chuẩn đã hoàn thiện (1 Video nổi bật lớn + 4 Video phụ dạng 2x2 + Tab filter + Cụm cam kết + Marketing "Tại sao nên xem?").
  - **Giao diện Tablet & Mobile ($\le 1024\text{px}$)**:
    + Section 6 (`[gobike_home_video_reviews]`): Tự động chuyển toàn bộ danh sách video thành dạng slide vuốt ngang (Swiper slider với `slidesPerView: 1.18` trên mobile, `1.5` trên 600px, `2.2` trên 768px), thẻ kế tiếp lộ mép phải (~18-20%). Mỗi thẻ có thumbnail 16:9 bo góc 12px, nút play mờ ở tâm, nhãn thời lượng góc dưới phải, tiêu đề và mô tả gọn gàng kèm nút 3 chấm tùy chọn và nút mũi tên tròn chuyển slide màu trắng đúng 100% ảnh thiết kế.
    + Section 4 (`[gobike_experience_videos]`): Chuyển lưới tĩnh 2 cột thành dạng slide vuốt ngang mượt mà (`overflow-x: auto; scroll-snap-type: x mandatory;`), thẻ rộng 78% lộ thẻ kế tiếp, giữ nguyên toàn bộ tính năng phát video Shorts trực tiếp và thêm giỏ hàng WooCommerce AJAX.

---

## 7. SECTION: TIN TỨC & KINH NGHIỆM SỬ DỤNG (TAB PILLS)
- [x] **Lỗi 1 (Bỏ phần vệt xanh ở tiêu đề TIN TỨC)**: 
  - Khắc phục: Xóa bỏ thanh chỉ thị xanh đứng (`::before` nền `#149d29`) phía trước tiêu đề `TIN TỨC` theo đúng yêu cầu, giữ tiêu đề tối giản và thanh lịch.
- [x] **Lỗi 2 (Thêm khoảng cách cho các khối bài viết phụ bên phải)**: 
  - Khắc phục: Bổ sung khoảng cách đệm trên và dưới (`padding-top: 14px; padding-bottom: 14px;`) cho từng bài viết phụ ở cột phải kết hợp đường line mờ `#f1f5f9`, tăng khoảng cách giữa ảnh và nội dung text (`gap: 16px`), giúp bố cục thông thoáng, các tin tức không còn bị dính sát nhau. 

---

## 8. SECTION: HỆ THỐNG SHOWROOM & CAM KẾT DỊCH VỤ
- [ ] **Lỗi 1**: 
- [ ] **Lỗi 2**: 

---

## 9. FOOTER & THANH ĐIỀU HƯỚNG DƯỚI (MOBILE BOTTOM BAR)
- [ ] **Lỗi 1**: 
- [ ] **Lỗi 2**: 

---

## 10. GHI CHÚ CHUNG & CÁC LỖI KHÁC TRÊN TRANG CHỦ
- 
